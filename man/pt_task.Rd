% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pt-task.R
\name{pt_task}
\alias{pt_task}
\title{The plottwist task}
\usage{
pt_task(
  epochs = 1,
  dir = vitals::vitals_log_dir(),
  samples = seq_len(nrow(pt_dataset))
)
}
\arguments{
\item{epochs}{Number of evaluation epochs to run. Default is 1.}

\item{dir}{Directory for logging evaluation results. Default is
\code{vitals::vitals_log_dir()}.}

\item{samples}{Row indices from \code{pt_dataset} to evaluate. Default is
all rows.}
}
\value{
A \link[vitals:Task]{vitals::Task} object. Call \verb{$eval(solver_chat = chat)} to run
the evaluation, where \code{chat} is an ellmer Chat object. The scorer uses
Claude Sonnet 4.6 by default; pass \code{scorer_chat} to override.
}
\description{
Combines \link{pt_dataset}, \link{pt_solver}, and \link{pt_scorer} into a \link[vitals:Task]{vitals::Task}
for evaluating language models' ability to detect and fix data quality
issues that show up in plots.
}
\seealso{
\link{pt_dataset}, \link{pt_solver}, \link{pt_scorer}, \link[vitals:Task]{vitals::Task}
}
