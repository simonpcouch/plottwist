% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pt-solver.R
\name{pt_solver}
\alias{pt_solver}
\title{The plottwist solver}
\usage{
pt_solver(inputs, ..., solver_chat)
}
\arguments{
\item{inputs}{List of input tibbles from \code{pt_dataset$input}.}

\item{...}{Additional arguments (currently unused).}

\item{solver_chat}{An ellmer Chat object. Each sample gets a fresh clone
with a randomized system prompt and tool harness.}
}
\value{
A list with components \code{result} (character vector of final responses)
and \code{solver_chat} (list of Chat objects).
}
\description{
Walks a language model through a multi-turn data analysis conversation.
For each sample, the solver copies the data file into a temporary directory,
creates an agent with randomly generated prompting, and chats through each
turn (picking one phrasing at random). The model's final response is
returned as the result.
}
