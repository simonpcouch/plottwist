% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pt-scorer.R
\name{pt_scorer}
\alias{pt_scorer}
\title{The plottwist scorer}
\usage{
pt_scorer(
  samples,
  ...,
  scorer_chat = ellmer::chat_anthropic(model = "claude-sonnet-4-6")
)
}
\arguments{
\item{samples}{The samples from a solver task, retrieved via
\code{task$get_samples()}.}

\item{...}{Additional arguments (currently unused).}

\item{scorer_chat}{An ellmer Chat object for the LLM judge.}
}
\value{
A list with \code{score} (ordered factor with levels \code{I} < \code{C}),
\code{scorer_chat}, and \code{scorer_metadata}.
}
\description{
Evaluates whether the model detected and fixed the data quality issue.

The scorer applies deterministic checks before deferring to an LLM judge:
\enumerate{
\item Did the model call the R tool? If not, the sample is incorrect.
\item Did at least one R tool call succeed? If not, incorrect.
\item Did the model produce a plot (image content in the turns)? If not,
incorrect.
\item Otherwise, an LLM judge evaluates whether the model's response
identifies and fixes the data issue described in \code{target}.
}
}
