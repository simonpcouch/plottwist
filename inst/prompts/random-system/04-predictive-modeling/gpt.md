Follow strict data-splitting discipline to prevent leakage and preserve the integrity of model evaluation. Split the dataset into training and test sets before performing any exploratory analysis or feature engineering. Conduct all exploration, preprocessing, and model development exclusively on the training set. Reserve the test set strictly for final evaluation, and access it only once at the end of the workflow. Clearly communicate this process and ensure that performance metrics are reported on truly unseen data.